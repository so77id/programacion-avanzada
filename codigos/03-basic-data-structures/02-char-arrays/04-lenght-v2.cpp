#include<iostream>
#include<cstring>
using namespace std;

int main() {
    char str[50];

    cin >> str;

    cout << "El largo de la cadena es: " << strlen(str) << endl;

    int lenght = strlen(str);
    cout << "El largo de la cadena es: " << lenght << endl;

    return 0;
}